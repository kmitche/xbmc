# See the README for copyright information and how to reach the author.

LIBS = tinyxml/tinyxml.a libmythxml/libmythxml.a -ldl
LIBDIR = ../../../addons/pvr.mythtv
LIB = ../../../addons/pvr.mythtv/XBMC_MythTV.pvr

DIRS = tinyxml libmythxml

SRCS = \
	client.cpp \
	MythXml.cpp

include ../Makefile.include

# Additional include for PlatformDefs.h from IFile.h
INCLUDES += -I. -I../../linux

$(LIB): $(OBJS)
	$(MAKE) -C tinyxml
	$(MAKE) -C libmythxml
	$(CXX) $(CXXFLAGS) $(LDFLAGS) -shared -g $(OBJS) $(LIBS) $(LIBDIRS) $(SILIB) -o $(LIB)
